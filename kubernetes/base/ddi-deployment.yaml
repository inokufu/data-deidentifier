---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ddi
  labels:
    app: ddi
    component: api
spec:
  selector:
    matchLabels:
      app: ddi
      component: api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: ddi
        component: api
    spec:
      containers:
        - name: ddi
          image: ddi  # Generic name without tag (will be set by Kustomize)
          envFrom:
            - configMapRef:
                name: ddi-config
          ports:
            - containerPort: 8005
              name: http
          resources:
            limits:
              cpu: "1"
              memory: "512Mi"
            requests:
              cpu: "200m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8005
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
