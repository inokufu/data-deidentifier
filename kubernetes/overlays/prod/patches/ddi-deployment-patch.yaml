---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ddi
spec:
  template:
    spec:
      imagePullSecrets:
        - name: ddi-scaleway-secret
      containers:
        - name: ddi
          imagePullPolicy: Always
          resources:
            limits:
              cpu: "800m"
              memory: "2.2Gi"
            requests:
              cpu: "500m"
              memory: "2Gi"
