---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ddi
spec:
  template:
    spec:
      imagePullSecrets:
        - name: ddi-scaleway-secret
      containers:
        - name: ddi
          imagePullPolicy: Always
          resources:
            limits:
              cpu: "2"
              memory: "2.5Gi"
            requests:
              cpu: "500m"
              memory: "1.5Gi"
